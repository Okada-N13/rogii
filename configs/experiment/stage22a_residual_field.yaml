experiment_name: stage22a_disjoint_candidate_residual_field
seed: 42

provenance:
  stage16b_manifest_sha256: af748e7092b8a605a756f478ebad80a95286631d8214a32d48f6af59b82b579c

candidates:
  gr_sigma_multipliers: [1.0, 1.3, 1.6]
  polynomial_degrees: []
  selector:
    particles: 64
    seeds: 4
    seed_base: 0
    maximum_tracking_steps: 384
    typewell_grid_step: 0.2
  top_pf_proxy:
    ridge_weight: 0.30
    selector_weight: 0.70
    projection_degree: 3
    projection_blend_weight: 0.75
    final_sp45_weight: 0.60

model:
  base_candidate: top_pf_a130
  training_stride: 4
  target_cap_ft: 24.0
  learning_rate: 0.05
  max_iter: 180
  max_leaf_nodes: 31
  min_samples_leaf: 80
  l2_regularization: 2.0

correction:
  primary_weight: 0.25
  diagnostic_weights: [0.10, 0.20, 0.25, 0.30, 0.40]
  cap_ft: 8.0
  ramp_rows: 96.0

validation:
  bootstrap_resamples: 3000
  minimum_gain: 0.10
  minimum_improved_folds: 4
  minimum_improved_fractions: 4
  minimum_spatial_folds: 4
  minimum_typewell_folds: 4
  minimum_branch_folds: 4
