experiment_name: quadratic_tvt
seed: 42

data:
  data_dir: null
  split: train
  limit_wells: null

cv:
  n_splits: 5
  seed: 42

model:
  name: guarded_trend
  target: tvt
  degree: 2
  window_ft: 1000
  minimum_points: 30
  ridge: 0.01
  huber_delta: 1.5
  robust_iterations: 5
  slope_clip: 0.03
  curvature_clip: 0.00001
  max_delta_ft: 60
  strength: 0.5

output:
  artifact_dir: null

