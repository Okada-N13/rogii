experiment_name: stage21b_disjoint_prefix_confidence_gate
seed: 42

provenance:
  stage16b_manifest_sha256: af748e7092b8a605a756f478ebad80a95286631d8214a32d48f6af59b82b579c

sample:
  cuts_per_stratum: 4

calibration:
  minimum_calibration_gap_rows: 96
  minimum_holdout_rows: 32
  inner_gap_fractions: [0.45, 0.72]
  residual_mad_weight: 0.5

candidates:
  gr_sigma_multipliers: [1.0, 1.3, 1.6]
  polynomial_degrees: []
  selector:
    particles: 64
    seeds: 4
    seed_base: 0
    maximum_tracking_steps: 384
    typewell_grid_step: 0.2
  top_pf_proxy:
    ridge_weight: 0.30
    selector_weight: 0.70
    projection_degree: 3
    projection_blend_weight: 0.75
    final_sp45_weight: 0.60

router:
  base_candidate: top_pf_a130
  blend_weight: 0.10
  cap_ft: 8.0
  ramp_rows: 96.0
  minimum_corrected_margin: 0.50
  minimum_inner_margin: 0.0

diagnostic_weights: [0.05, 0.10, 0.15, 0.20]

validation:
  n_typewell_folds: 5
  bootstrap_resamples: 3000
  minimum_gain: 0.05
  minimum_improved_folds: 4
  minimum_improved_fractions: 4
  minimum_spatial_folds: 4
  minimum_typewell_folds: 4
  minimum_branch_folds: 4
